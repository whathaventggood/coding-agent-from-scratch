# Coding Agent 演示与量化记录

更新日期：2026-09-24。本文区分已经观察到的结果与尚待补跑的实验；它不是稳定成功率声明。

## 一分钟演示

项目提供三个安装后命令：`coding-agent` 执行任务，`coding-agent-benchmark` 在隔离的临时 Git 工作区运行固定任务集，`coding-agent-evaluate` 汇总运行报告。典型闭环是：读取/搜索工作区 → 模型选择受控工具 → 显式授权后修改文件 → 运行本地白名单测试 → CLI 独立复验 → 输出工具轨迹、文件变化和 JSON 报告。失败时可由调用者显式选择恢复任务开始时的 Git 可见普通文件状态；不把它描述成沙箱或任意文件的事务回滚。

离线演示先运行：

```bash
.venv/bin/python -m pytest -q
.venv/bin/coding-agent --help
.venv/bin/coding-agent-benchmark --help
.venv/bin/coding-agent-evaluate --help
```

真实模型演示需要调用者在自己的终端配置 `DEEPSEEK_API_KEY`。每次使用一个尚不存在的输出目录：

```bash
.venv/bin/coding-agent-benchmark benchmark_tasks.json \
  --output-dir /tmp/coding-agent-benchmark-001
```

该命令调用真实模型、可能产生费用；临时任务工作区在任务结束后清理，输出目录保留每个任务的 JSON 报告和 `summary.json`。不要把密钥、私人工作区内容或未经检查的原始报告提交到仓库。`summary.json` 是两项固定任务的汇总，不是独立盲测集。需要重复试验时使用新目录编号，保留失败运行，再用 `coding-agent-evaluate` 汇总全部逐任务 JSON；不要只挑成功样本。

## 已观察到的真实模型结果

2026-09-23，同一份两任务清单、同一 DeepSeek 模型、每任务最多 8 次模型请求，分别在工作流指导加入前后运行一次。历史执行记录保存在工作区根目录的《学习规划与进度.md》；原始逐任务 JSON 未纳入仓库，因此下表属于有文字交接、但无法从本仓库原始报告重新计算的历史结果。

| 条件 | 任务成功且独立复验通过 | 模型请求总数 | 工具错误 | 最大消息峰值（近似字符） |
| --- | ---: | ---: | ---: | ---: |
| 工作流指导前 | 1/2 | 13 | 2 | 3205 |
| 工作流指导 v1 | 2/2 | 13 | 1 | 4721 |

关键案例是 `create-text-normalizer`：首次耗尽 8 次请求仍未创建缺失模块；加入工作流指导后，第 5 次请求创建文件，第 6 次执行定向测试，第 7 次执行完整测试，第 8 次返回完成，随后 CLI 独立复验通过。工作流指导要求在测试/导入指出缺失源文件且允许编辑时考虑创建文件，并避免无新证据地重复失败读取。消息峰值同时升高，说明这不是无代价的改进。

这个前后对比只能说明两次具体执行的差别。每种条件仅有一次、每次只有两个固定任务；不能把 2/2 宣称为总体 100% 成功率，也不能从中推断改进具有统计显著性。模型输出非确定性，任务集规模、难度与真实仓库也有差距。

### 2026-09-24 当前主线五轮复测

用户以 `2773768` 为 HEAD 连续运行 5 轮同一固定任务集，共 10 次任务执行；工作区另有两处未提交的用户学习注释，核对差异后确认没有改动执行语句。代码指定的模型为 `deepseek-flash`；清单 SHA-256 为 `a615a9db466393557eff291ec3020e05be07b0685c7a1a311ebd651ce6b7633b`。10 份报告均记录每任务最多 8 次模型请求、`pytest` 验证方案，未记录对测试文件的修改。每轮的两份原始 JSON 和 `summary.json` 保留在本机 `/private/tmp/coding-agent-benchmark-20260924-01/` 至 `-05/`，未提交到仓库。

| 轮次 | 成功且独立复验通过 | 模型请求 | 工具错误 | 最大消息峰值（近似字符） |
| --- | ---: | ---: | ---: | ---: |
| 01 | 1/2 | 13 | 2 | 4556 |
| 02 | 2/2 | 10 | 1 | 2924 |
| 03 | 2/2 | 13 | 1 | 3963 |
| 04 | 1/2 | 13 | 0 | 3136 |
| 05 | 2/2 | 13 | 0 | 3856 |
| 合计 | **8/10（观察值 80%）** | **62** | **4** | **4556** |

`fix-addition` 为 5/5 成功；`create-text-normalizer` 为 3/5。10 次执行共 64 次工具调用；8 次成功均通过 CLI 独立复验，2 次失败均跳过该复验，没有独立复验失败。失败均发生在 `create-text-normalizer` 且原因是“达到最大步骤数”：01 轮第 7 次请求创建文件、第 8 次定向 pytest 通过，但没有额度让模型接收结果并回答；04 轮第 8 次请求才创建文件，没有测试或最终回答。两次失败报告都记录了新建 `text_utils.py`，但临时工作区结束后清理，文件变化不等于任务完成。不能将 01 轮的工具测试成功或 04 轮的文件创建算作通过。

这 5 轮只覆盖两个简单固定任务，80% 是这 10 次执行的观察比例，不是总体成功率保证；更不能与历史各一次的 1/2、2/2 当成统计显著的前后对照。两次失败指向步骤预算与无效探索的组合问题。后续改进应保持测试文件、验证方案和预算不变先优化工具选择；若提高预算，须作为另一组实验单独报告。

### 预算感知工作流提示 v2：五轮对照

用户随后以本地提交 `27746de` 为 HEAD 再运行 5 轮；本机仍有两处仅含学习注释的未提交差异。固定任务清单哈希保持 `a615a9db466393557eff291ec3020e05be07b0685c7a1a311ebd651ce6b7633b`，模型仍由代码指定为 `deepseek-flash`，全部 10 份报告均记录每任务最多 8 次模型请求和 `pytest` 方案，未修改测试文件。新提示只告知请求预算、要求预留最终回答机会，并在缺失文件证据明确时减少重复探索；工具权限和 CLI 独立复验未变。原始报告保留在本机 `/private/tmp/coding-agent-benchmark-budget-v2-01/` 至 `-05/`，未提交到仓库。

| 轮次 | 成功且独立复验通过 | 模型请求 | 工具错误 | 最大消息峰值（近似字符） |
| --- | ---: | ---: | ---: | ---: |
| 01 | 2/2 | 12 | 1 | 4142 |
| 02 | 2/2 | 10 | 0 | 2505 |
| 03 | 2/2 | 11 | 1 | 3295 |
| 04 | 2/2 | 12 | 1 | 4060 |
| 05 | 2/2 | 12 | 2 | 4278 |
| 合计 | **10/10（本样本观察值）** | **57** | **5** | **4278** |

新提示下 `fix-addition` 与 `create-text-normalizer` 均为 5/5，10 次任务都经 CLI 独立复验通过；共 60 次工具调用。相比上一组同条件五轮，观察到成功任务由 8/10 变为 10/10、模型请求由 62 降到 57、工具调用由 64 降到 60，但工具错误由 4 增到 5。消息峰值是整组最大值，并非平均耗用。两组都只有两个简单任务、各五轮，模型输出非确定性；这些数据支持“本次对照样本表现更好”，不足以证明提示必然导致改善，更不能宣称真实仓库任务的稳定 100% 成功率。

## 当前可复现的工程证据

新增八题作品集评测与历史两题集分开运行。八题均指定可见测试文件为
`protected_files`，八题均配置了只在 Agent 完成后加入工作区的
`holdout_files`。评测器会区分 CLI 原始成功与守门后的最终成功；
修改可见测试或留出测试失败均不能计为最终成功。留出测试写在公开清单中，
仅对本次模型执行的临时工作区不可见，不是私有盲测。当前只有离线基线与
守门代码测试，**没有八题真实模型结果**，所以下面的历史 8/10、10/10
仍只属于旧两题集，不能移植到新任务集。

- 提交 `79e6549` 的 [GitHub Actions 运行](https://github.com/whathaventggood/coding-agent-from-scratch/actions/runs/36002473855) 已在 Python 3.11 和 3.14 上分别完成安装、离线回归和三个安装后命令的项目外烟雾检查，两个任务均成功。CI 不调用真实模型，也不能替代真实评测。
- 本地该提交的离线回归为 94 项通过，标准 wheel 安装和项目外三个命令入口均已验证。这里的结果说明软件回归，不代表模型任务成功率。
- 固定任务清单为 `benchmark_tasks.json`；任务一修复错误的加法实现，任务二创建缺失的文本处理模块。两个任务都在独立临时 Git 工作区运行，并以 CLI 的完整 pytest 结果作为独立复验。

## 下一次量化更新的验收口径

两组各五轮的同条件复测已完成。下一次改动后的对照实验仍应记录提交号、任务清单哈希、模型、每任务请求上限、运行次数、任务成功数、独立复验通过数、失败类型、工具错误数和请求数；保留全部失败样本，不只挑成功样本。逐任务 JSON 应作为原始证据保留在本地；如果需要公开，只公开已审查和脱敏的报告。不同版本对比时保持任务、预算、验证方案一致，并同时报告成本与失败样本。

Agent 所在会话未配置 `DEEPSEEK_API_KEY`；2026-09-24 两组各五轮均由用户在自己的终端运行。
